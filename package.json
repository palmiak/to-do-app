{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "cd server && npm install",
    "start": "node ./server/server.js",
    "client": "cd client && npm install && npm start"
  }
}
